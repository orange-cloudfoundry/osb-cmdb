# Potentially used by developers to instruct test classes to use a local cloudfoundry environment
spring:
  cloud:
    appbroker:
      deployer:
        cloudfoundry:
          api-host: api.cf.test
          api-port: 80
          username: admin
          password: adminpass
          default-org: test
          default-space: development
          skip-ssl-validation: true